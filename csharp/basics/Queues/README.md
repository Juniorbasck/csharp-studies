# 📚 Queues em C# (`Queue<T>`) - A Base do BFS

A Fila (Queue) é uma estrutura de dados FIFO (Primeiro a Entrar, Primeiro a Sair). É a estrutura ideal para algoritmos que precisam explorar dados em "camadas" ou níveis, como a Busca em Largura (BFS).

## 1. Declaração e Inicialização
```csharp
Queue<int> fila = new Queue<int>();